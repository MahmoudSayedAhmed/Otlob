<%= form_with(model: invited, local: true) do |form| %>
  <% if invited.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(invited.errors.count, "error") %> prohibited this invited from being saved:</h2>

      <ul>
      <% invited.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :order_id %>
    <%= form.text_field :order_id, id: :invited_order_id %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :invited_user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
