<h1>Friends</h1>
<div>
	<span>"Enter your friend email" </span>
	<input  class="field " type="email" id="addfriend">
	<button  class="field" type="button" onclick="addFriend()">Add</button>
<div>
<h3>Friends List</h3>
<div id="friendsList">
	<% @friends.each do |friend|%>
		<div>
			<%= image_tag User.find(friend.friend_id).uimage (:thumb)  %>
	        <p><%= User.find(friend.friend_id).name %></p>
	        <p id="<%=friend.friend_id%>" onclick="removeFriend(this)" class="removeLink">unfriend</p>
	    </div>
    <% end %>
</div>
